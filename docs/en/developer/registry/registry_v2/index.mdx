---
weight: 20
i18n:
  title:
    en: "Registry v2"
---

# Registry v2: Operator-Managed ACP Registry

This section describes **Registry v2**, the Operator-managed <Term name="productShort" /> Registry deployed and reconciled by `cluster-image-registry-operator`. Registry v2 follows the same information architecture as the OpenShift Container Platform 4.21 Registry documentation: overview, Image Registry Operator, setting up and configuring the registry, accessing the registry, and exposing the registry. ACP-specific image management and migration content is added as separate pages.

The legacy Registry documentation remains available under [How To](../how_to/index.mdx) for environments that still use the earlier `cpaas-system` Registry service.

## Documentation Structure

| OCP 4.21 Registry chapter | ACP Registry v2 page |
| --- | --- |
| OpenShift image registry overview | [ACP image registry overview](./registry_overview.mdx) |
| Image Registry Operator in OpenShift Container Platform | [Image Registry Operator](./image_registry_operator.mdx) |
| Setting up and configuring the registry | [Setting up and configuring the registry](./setting_up_and_configuring_the_registry.mdx) |
| Accessing the registry | [Accessing the registry](./accessing_the_registry.mdx) |
| Exposing the registry | [Exposing the registry](./exposing_the_registry.mdx) |
| ACP-specific `ac` image management topic | [Managing images with ac](./managing_images_with_ac.mdx) |
| ACP-specific migration topic | [Migrating from the legacy ACP Registry](./migrating_from_legacy_registry.mdx) |

## Standard Addresses

Use the Registry v2 internal service address for workloads running in the cluster:

```text
image-registry.image-registry-system.svc:5000
```

The short service address can be used for compatibility validation when node DNS and trust configuration support it:

```text
image-registry.image-registry-system:5000
```

Use an external Ingress hostname for developer machines, CI systems, and other clients outside the cluster.

<Overview />
