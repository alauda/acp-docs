---
weight: 20
i18n:
  title:
    en: "Registry v2"
---

# Registry v2: Operator-Managed ACP Registry

This section describes **Registry v2**, the Operator-managed <Term name="productShort" /> image registry deployed and reconciled by `cluster-image-registry-operator`.

The page structure follows the ACP Registry v2 workflow: overview, Image Registry Operator, setting up and configuring the registry, accessing the registry, exposing the registry, image management, and legacy migration.

The legacy Registry documentation remains available under [How To](../how_to/index.mdx) for environments that still use the earlier `cpaas-system` Registry service.

## Documentation Structure

| Registry topic | ACP Registry v2 page |
| --- | --- |
| ACP image registry overview | [ACP image registry overview](./registry_overview.mdx) |
| Image Registry Operator | [Image Registry Operator](./image_registry_operator.mdx) |
| Setting up and configuring the registry | [Setting up and configuring the registry](./setting_up_and_configuring_the_registry.mdx) |
| Accessing the registry | [Accessing the registry](./accessing_the_registry.mdx) |
| Exposing the registry | [Exposing the registry](./exposing_the_registry.mdx) |
| ACP-specific `ac` image management topic | [Managing images with ac](./managing_images_with_ac.mdx) |
| ACP-specific migration topic | [Migrating from the legacy ACP Registry](./migrating_from_legacy_registry.mdx) |

## Standard Address

Use the fully qualified internal service address for workloads running in the cluster:

```text
image-registry.image-registry-system.svc:5000
```

Use an external Ingress hostname only for developer machines, CI systems, or other clients outside the cluster. See [Exposing the registry](./exposing_the_registry.mdx).

<Overview />
