---
weight: 10
queries:
  - How do I choose between Local Storage LVM Storage and Ceph
  - Difference between Local Storage and LVM Storage
  - Storage systems comparison Alauda Container Platform
---

# Overview

Alauda Container Platform documents three storage systems. Choose one based on how volumes are provisioned and whether data must survive a node failure.

Do not use Local Storage and LVM Storage as interchangeable names. Local Storage is the static local-disk provisioner. LVM Storage is the TopoLVM-based dynamic provisioner.

## Choose a storage system

| Storage system | Provisioning | Typical use | Data protection |
| --- | --- | --- | --- |
| **Local Storage** | Static. One PersistentVolume per listed disk or partition. | Expose a specific local disk to a workload, including as a Block volume consumed by another storage system. The Marketplace package name is **Alauda Build of LocalStorage**. | A node failure makes the volume unavailable. A disk failure can cause data loss. |
| **LVM Storage** | Dynamic. TopoLVM carves logical volumes from node volume groups. | Databases and caches that need low-latency local volumes and PVC resize. The Marketplace package name remains **Alauda Build of TopoLVM**. | A node failure makes the volume unavailable. A disk failure can cause data loss. |
| **Ceph Distributed Storage** | Dynamic. Replicated or erasure-coded pools. | Shared block, file, and object storage that must survive a node failure. | Replicas or erasure coding across nodes. |

## Related information

- [Local Storage](./local_storage/index.mdx)
- [LVM Storage](./storagesystem_topolvm/index.mdx)
- [Ceph Distributed Storage](./storagesystem_ceph/index.mdx)
