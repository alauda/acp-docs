---
weight: 15
queries:
  - How do I install Alauda Build of LocalStorage
  - Local Storage operator install channel and namespace
  - Install Local Storage without creating LocalVolume
---

# Install

Install **Alauda Build of LocalStorage** so the cluster can inventory local disks and later provision PersistentVolumes from them.

This page covers only the operator. After installation, use [Guides](./functions/index.mdx) to discover devices and to create PersistentVolumes. `LocalVolume` and `LocalVolumeSet` provisioning is not part of operator installation.

## Prerequisites

- **Download** the **Alauda Build of LocalStorage** installation package corresponding to your platform architecture.
- **Upload** the **Alauda Build of LocalStorage** installation package using the Upload Packages mechanism.

## Procedure

<Steps>

### Install Alauda Build of LocalStorage

1. Login, go to the **Administrator** page.

2. Click **Marketplace** > **OperatorHub** to enter the **OperatorHub** page.

3. Find the **Alauda Build of LocalStorage**, click **Install**, and navigate to the **Install Alauda Build of LocalStorage** page.

    Configuration Parameters:
    | **Parameter**   | **Recommended Configuration**                          |
    | :-------------- | :----------------------------------------------------- |
    | **Channel**     | The default channel is `stable`.                       |
    | **Installation Mode**  | `Cluster`: All namespaces in the cluster share a single Operator instance for creation and management, resulting in lower resource usage. |
    | **Installation Place** | Select `Recommended`, Namespace only support **acp-storage**.|
    | **Upgrade Strategy**   | `Manual`: When there is a new version in the Operator Hub, manual confirmation is required to upgrade the Operator to the latest version. |

</Steps>

## Next steps

- Discover available disks and provision PersistentVolumes in [Guides](./functions/index.mdx).
- LVM Storage and Ceph Internal Mode **Selection Device** use this operator for discovery. See [Install LVM Storage](../storagesystem_topolvm/installation.mdx) and [Deploying in Internal Mode](../storagesystem_ceph/installation/create_service_stand.mdx).
- To back Ceph OSDs with Local Storage Block volumes, provision volumes in [Guides](./functions/index.mdx), then see [Deploying in Internal Mode with Local Storage](../storagesystem_ceph/installation/create_service_stand_local_storage.mdx).
