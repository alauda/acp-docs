---
weight: 10
i18n:
  additionalPrompts: |
    Keep every markdown link URL exactly unchanged. In particular, do not change `../../virtualization/virtualization/virtual_machine/functions/create_virtual_machine.mdx`.
---

# GPU

Use this page when you want to run container workloads on GPUs or choose the right GPU product path in ACP.

Use the NVIDIA vendor integration for hardware enablement, direct GPU allocation, DRA, GPU Operator, or NVIDIA metrics. Use HAMi when the user task starts from sharing, virtualization, or scheduling behavior built on a GPU base.

## Choose a GPU path

| Need | Typical path |
| --- | --- |
| Use NVIDIA GPUs with vendor-standard components | NVIDIA vendor integration |
| Share, virtualize, or schedule GPU resources across workloads | HAMi sharing and scheduling path |
| Use claim-based GPU allocation | DRA, when the target product delivery explicitly supports a DRA driver |
| Use GPU passthrough for virtual machines | [ACP Virtualization virtual machine creation documentation](../../virtualization/virtualization/virtual_machine/functions/create_virtual_machine.mdx) |

## Containers and VMs

Container workload GPU usage and virtual machine GPU passthrough are different paths.

- Container workload GPU usage is covered by this accelerator section and related product sites.
- If a GPU resource is allocatable on the node but not visible in ACP quota pages, register the accelerator quota field metadata in [Accelerator resource quota](../mechanisms/resource-quota.mdx).
- Virtual machine physical GPU passthrough is configured in [ACP Virtualization documentation](../../virtualization/virtualization/virtual_machine/functions/create_virtual_machine.mdx).

## Related pages

- [NVIDIA vendor integration](../products/vendor-integrations/nvidia.mdx)
- [HAMi sharing and scheduling path](../products/sharing-and-scheduling/hami.mdx)
- [Device Plugin](../mechanisms/device-plugin.mdx)
- [DRA](../mechanisms/dra.mdx)
