---
weight: 30
---

# Upgrade Workload Clusters

Workload clusters can be upgraded after the global tier has already reached the target ACP Distribution Version. The global tier does not need to be upgraded again if it is already at that Distribution Version.

ACP 4.3 uses the same CVO-based workflow for workload clusters as for the `global` cluster, but with two additional rules:

- If the platform uses **global DR**, both the standby and primary global clusters must reach the target Distribution Version before any workload cluster is upgraded to that Distribution Version.
- The workload cluster normally exposes the target version in `availableUpdates` only after the global tier has already reached that same Distribution Version.

## Workflow

<Steps>

### Confirm workload-cluster prerequisites

Before requesting the upgrade, verify that:

* The global tier has already reached the target ACP Distribution Version.
* In global DR environments, both the standby and primary global clusters have already reached that target Distribution Version.
* The target version is visible in `availableUpdates` for the workload cluster.

### Request the upgrade

After the target version becomes available for the workload cluster, choose one of the following entry points:

* Request the target version from the Web Console on the workload cluster details page.
* Use ACP CLI when your current ACP context already points to the target workload cluster and the cluster exposes upgrade information to ACP CLI.

Web Console path:

```text
Administrator > Clusters > Clusters > <workload cluster> > Functional Components
```

ACP CLI example:

```bash
ac adm upgrade
ac adm upgrade --to-latest=true
ac adm upgrade --to=4.3.0
ac adm upgrade status
```

ACP CLI submits the requested target from the current ACP context. Whether the upgrade can proceed is still determined by the cluster's available updates and the upgrade controller's preflight checks. ACP CLI is not used to upgrade the `global` cluster.

### Observe progress

After the upgrade request is submitted, use `cvsh.status` to track current version, desired version, preflight results, stages, and history:

```bash
kubectl get cvsh <cluster> -n cpaas-system
kubectl get cvsh <cluster> -n cpaas-system -o yaml
```

When troubleshooting, inspect conditions, preflight details, and history first:

```bash
kubectl -n cpaas-system get cvsh <cluster> \
  -o jsonpath='{range .status.conditions[*]}{.type}{"\t"}{.status}{"\t"}{.reason}{"\t"}{.message}{"\n"}{end}'

kubectl -n cpaas-system get cvsh <cluster> \
  -o jsonpath='{.status.preflight.observedAt}{"\n"}{range .status.preflight.checks[*]}{.name}{"\t"}{.policy}{"\t"}{.state}{"\t"}{.reason}{"\t"}{.message}{"\n"}{end}'

kubectl -n cpaas-system get cvsh <cluster> \
  -o jsonpath='{range .status.history[*]}{.version}{"\t"}{.state}{"\t"}{.startedTime}{"\t"}{.completionTime}{"\n"}{end}'
```

</Steps>

## Related Documentation

- [Overview](./overview.mdx)
- [Upgrade the global cluster](./upgrade_global_cluster.mdx)
