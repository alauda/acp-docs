---
weight: 50
i18n:
  title:
    en: Создание приложений из YAML
    zh: 通过 YAML 创建应用
title: Создание приложений из YAML
sourceSHA: 157824b7d24e3db29e6ce567a7e58fb1c4c312084a925e2881e64b0f1713f9b7
---

# Создание приложений из YAML

Если вы владеете синтаксисом YAML и предпочитаете использовать конфигурации, не определенные в формах или шаблонах, вы можете воспользоваться методом создания YAML одним щелчком. Этот подход позволяет более гибко настраивать основную информацию и ресурсы для вашего нативного приложения.

## Предостережения

Когда в кластере существуют как Linux, так и Windows узлы, чтобы предотвратить назначение приложения на несовместимые узлы, необходимо настроить выбор узлов. Например:

```yaml
spec:
    spec:
      nodeSelector:
        kubernetes.io/os: linux
```

## Предварительные требования

Убедитесь, что образы, определенные в YAML, могут быть загружены в текущем кластере. Вы можете проверить это с помощью команды `docker pull`.

## Процедура

1. **Платформа контейнеров**, и перейдите в **Приложение** > **Приложения**.

2. Нажмите **Создать**.

3. Выберите **Создать из YAML**.

4. Завершите конфигурацию и нажмите **Создать**.

5. Соответствующий **Развертывание** можно просмотреть на странице деталей.

````yaml
# webapp-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: webapp
  labels:
    app: webapp
    env: prod
spec:
  replicas: 3
  selector:
    matchLabels:
      app: webapp
  template:
    metadata:
      labels:
        app: webapp
        tier: frontend
    spec:
      containers:
      - name: webapp
        image: nginx:1.25-alpine
        ports:
        - containerPort: 80
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "250m"
            memory: "256Mi"
---
# webapp-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: webapp-service
spec:
  selector:
    app: webapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
```yaml
````
