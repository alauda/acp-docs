---
weight: 80
i18n:
  title:
    en: Создание приложений с использованием CLI
    zh: 通过 CLI 工具创建应用
title: Создание приложений с использованием CLI
sourceSHA: abde81d50f92fbd90796fca641dc9f7ce5e610cfc31aa73e752674ccae0400fe
---

# Создание приложений с использованием CLI

`kubectl` является основным интерфейсом командной строки (CLI) для взаимодействия с кластерами Kubernetes. Он функцииирует как клиент для API-сервера Kubernetes - RESTful HTTP API, который служит программным интерфейсом управляющей плоскости. Все операции Kubernetes представлены через API-эндпоинты, и `kubectl` по сути переводит команды CLI в соответствующие API-запросы для управления ресурсами кластера и рабочими нагрузками приложений (развертывания, наборы состояния и т.д.).

Инструменты CLI упрощают развертывание приложений, интеллигентно интерпретируя входные артефакты (образы, или Chart и т.д.) и генерируя соответствующие объекты API Kubernetes. Сгенерированные ресурсы варьируются в зависимости от типов входных данных:

- **Образ**: Непосредственно создает Deployment.
- **Chart**: Создает все объекты, определенные в Helm Chart.

## Предварительные требования

Плагин **ACP Web Terminal** установлен, и переключатель web-cli включен.

## Процедура

1. **Платформа контейнеров**, нажмите на иконку терминала в нижнем правом углу.

2. Подождите инициализацию сессии (1-3 сек).

3. Выполните команды kubectl в интерактивной оболочке:

```bash
   kubectl get pods -n ${CURRENT_NAMESPACE}
```

4. Просмотрите вывод команды в реальном времени

## Пример

### YAML

```yaml
# webapp.yaml
apiVersion: app.k8s.io/v1beta1
kind: Application
metadata:
  name: webapp
spec:
  componentKinds:
    - group: apps
      kind: Deployment
    - group: ""
      kind: Service
  descriptor: {}

# webapp-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: webapp
  labels:
    app: webapp
    env: prod
spec:
  replicas: 3
  selector:
    matchLabels:
      app: webapp
  template:
    metadata:
      labels:
        app: webapp
        tier: frontend
    spec:
      containers:
      - name: webapp
        image: nginx:1.25-alpine
        ports:
        - containerPort: 80
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "250m"
            memory: "256Mi"
---
# webapp-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: webapp-service
spec:
  selector:
    app: webapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
```

### Команды kubectl

```bash
kubectl apply -f webapp.yaml -n {CURRENT_NAMESPACE}
kubectl apply -f webapp-deployment.yaml -n {CURRENT_NAMESPACE}
kubectl apply -f webapp-service.yaml -n {CURRENT_NAMESPACE}
```

## Ссылки

- **Концептуальное руководство**: [Обзор kubectl](https://kubernetes.io/docs/reference/kubectl/overview/)
- **Справочник по синтаксису**: [Шпаргалка по kubectl](https://kubernetes.io/docs/reference/kubectl/cheatsheet/)
- **Руководство по командам**: [Команды kubectl](https://kubernetes.io/docs/reference/generated/kubectl/kubectl-commands)
