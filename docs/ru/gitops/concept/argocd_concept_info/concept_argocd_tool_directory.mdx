---
weight: 70
i18n:
  title:
    en: Directory
    zh: Directory 概念
    ru: Директория
title: Директория
sourceSHA: e8d7a40d5fe32bb85f84d87db6e2c3843acb713cf368ede32be799a98a9547b0
---

# Директория

## Введение

Приложение типа **Директория** загружает `манифесты` напрямую из файлов `.yml`, `.yaml` или `.json`. Приложения директории могут быть созданы через пользовательский интерфейс платформы, панель управления Argo CD, CLI или декларативно. Пример декларативного синтаксиса:

```yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: guestbook
spec:
  destination:
    namespace: default
    server: https://kubernetes.default.svc
  project: default
  source:
    path: guestbook
    repoURL: https://github.com/argoproj/argocd-example-apps.git
    targetRevision: HEAD
```

Поле spec.source.directory не требуется, если не нужны дополнительные параметры конфигурации. Argo CD автоматически определяет, содержит ли исходный репозиторий/путь обычные файлы манифеста.

## Преимущества

- **Простота**: Прямо загружает ресурсы из файлов манифеста без дополнительной абстракции.

- **Низкие затраты на обслуживание**: Без накладных расходов на управление конфигурацией.

## Сценарии использования

- Управление несколькими ресурсами Kubernetes (например, развертывания, службы, ConfigMaps).

- Небольшие проекты, минимальные ресурсы или быстрое внедрение GitOps.

- Развертывание сырых файлов YAML без динамического шаблонирования или сложного управления конфигурацией.

:::warning
Приложения типа Директория **поддерживают только обычные файлы манифеста**. Если Argo CD обнаруживает файлы `Kustomize`, `Helm` или `Jsonnet` в пути директории, он не сможет отрендерить манифесты.
:::

# Ссылки

Для более подробных инструкций см: [Директория](https://argo-cd.readthedocs.io/en/stable/user-guide/directory/)
