---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  creationTimestamp: null
  name: inspections.middleware.alauda.io
spec:
  group: middleware.alauda.io
  names:
    kind: Inspection
    listKind: InspectionList
    plural: inspections
    singular: inspection
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.component
      name: Component
      type: string
    - jsonPath: .spec.trigger
      name: Trigger
      type: string
    - jsonPath: .spec.schedule
      name: Schedule
      type: string
    - jsonPath: .spec.suspend
      name: Suspend
      type: boolean
    - jsonPath: .spec.jobsHistoryLimit
      name: Limit
      type: integer
    - jsonPath: .status.message
      name: Message
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: Inspection is the Schema for the inspections API, which is used to define and manage inspection tasks and their configurations within a Kubernetes namespace.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: InspectionSpec defines the desired state of an Inspection, including the component to be inspected, instance selection, scheduling, and other configuration parameters.
            properties:
              component:
                description: Component Type, indicating which kind of component this instance belongs to. It helps to categorize the inspection based on the component type.
                type: string
              instances:
                description: Instances list, use "*" for all instances. This list specifies which instances of the component will be included in the inspection.
                items:
                  description: InstanceSelector is used to select specific instances for the inspection.
                  properties:
                    name:
                      description: The name of the instance. Use "*" to represent all instances. It uniquely identifies an instance to be inspected.
                      type: string
                  required:
                  - name
                  type: object
                type: array
              jobsHistoryLimit:
                description: Job history upper-limit number. A value of 0 stands for unlimited. This parameter controls how many historical inspection job records are retained.
                type: integer
              schedule:
                description: Time trigger expression in cron format. Refer to https://en.wikipedia.org/wiki/Cron for more info. Time can only be accurate to the minute. It defines when the inspection job should be automatically triggered.
                type: string
              suspend:
                description: Whether to suspend this inspection from executing. Setting this to true will pause the inspection process.
                type: boolean
              trigger:
                description: The way of triggering, including `Manual` and `Schedule`. It determines how the inspection job is initiated.
                type: string
            required:
            - component
            - instances
            type: object
          status:
            description: InspectionStatus defines the observed status of an Inspection plan, including information about the last job execution and any failure reasons.
            properties:
              lastJobResult:
                description: Last inspection result. Valid values include `Succeed` and `Failed`. This array contains details about the result of the last inspection job.
                items:
                  description: Inspection result structure, which holds information about the inspection outcome for a specific instance.
                  properties:
                    healthyStatus:
                      description: Inspection result, indicating the health status of the inspected instance.
                      type: string
                    id:
                      description: Inspection ID, a unique identifier for this inspection operation.
                      type: string
                    instance:
                      description: Instance to be inspected, specifying the name of the instance that was inspected.
                      type: string
                    message:
                      description: Job failed reason. Will be empty if this job executed successfully. It provides details about why the inspection job failed.
                      type: string
                    startTime:
                      description: The execution starting time of this job, recorded in date - time format.
                      format: date-time
                      type: string
                    status:
                      description: Job execution status, including `running`, `success`, and `failed`. It shows the current state of the inspection job.
                      type: string
                    stopTime:
                      description: The execution stopped time of this job, recorded in date - time format.
                      format: date-time
                      type: string
                  required:
                  - id
                  - instance
                  - status
                  type: object
                type: array
              lastJobStartTime:
                description: The execution starting time of the last job, recorded in date - time format.
                format: date-time
                type: string
              lastJobStatus:
                description: Last job execution status, indicating whether the last inspection job succeeded or failed.
                type: string
              lastJobStopTime:
                description: The execution stopped time of the last job, recorded in date - time format.
                format: date-time
                type: string
              message:
                description: Failed reason when the last job failed. Will be empty if the job executed successfully. It provides a high - level explanation of the failure.
                type: string
            required:
            - message
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
