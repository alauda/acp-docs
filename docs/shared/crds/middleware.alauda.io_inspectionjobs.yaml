---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  creationTimestamp: null
  name: inspectionjobs.middleware.alauda.io
spec:
  group: middleware.alauda.io
  names:
    kind: InspectionJob
    listKind: InspectionJobList
    plural: inspectionjobs
    singular: inspectionjob
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .status.instanceCount
      name: Instance
      type: integer
    - jsonPath: .status.message
      name: Message
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: InspectionJob is the Schema for the inspectionjobs API, which is used to define and manage inspection tasks in the Kubernetes cluster.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: InspectionJobSpec defines the desired state of InspectionJob, including the component type to be inspected, the list of instances etc.
            properties:
              component:
                description: Component refers to the type of the component to be inspected.
                type: string
              instances:
                description: Instances is a list of instances to be inspected. Each item in the list represents an instance of `component`.
                items:
                  properties:
                    name:
                      description: Name is the name of the instance. Using "*" means all instances.
                      type: string
                  required:
                  - name
                  type: object
                type: array
            required:
            - component
            - instances
            type: object
          status:
            description: InspectionJobStatus defines the observed state of InspectionJob, including information such as the number of instances, messages, and inspection results etc.
            properties:
              instanceCount:
                description: InstanceCount represents the number of instances involved in the inspection.
                type: integer
              message:
                description: Message contains relevant information or notifications about the inspection job.
                type: string
              results:
                description: Results show the inspection results of each instance. Each item in the list is an inspection result summary.
                items:
                  description: InspectionAbstractResult summarizes the inspection result of an instance.
                  properties:
                    healthyStatus:
                      description: Status indicates the result of the inspection, such as "healthy", "unhealthy", etc.
                      type: string
                    id:
                      description: Id is a unique identifier for the inspection result.
                      type: string
                    instance:
                      description: Instance is the name of the inspected instance.
                      type: string
                    message:
                      description: Message provides detailed information about the inspection result when failed. Will be empty if succeed.
                      type: string
                    startTime:
                      description: StartTime is the time when the inspection of this instance started.
                      format: date-time
                      type: string
                    status:
                      description: Status represents the execution status of the inspection job for this instance, such as "running", "completed", "failed", etc.
                      type: string
                    stopTime:
                      description: StopTime is the time when the inspection of this instance ended.
                      format: date-time
                      type: string
                  required:
                  - id
                  - instance
                  - status
                  type: object
                type: array
              startTime:
                description: StartTime is the time when the entire inspection job started.
                format: date-time
                type: string
              status:
                description: Status represents the overall execution status of the inspection job, such as "running", "completed", "failed", etc.
                type: string
              stopTime:
                description: StopTime is the time when the entire inspection job ended.
                format: date-time
                type: string
            required:
            - message
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
