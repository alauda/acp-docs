---
weight: 10
sourceSHA: 3a7b6b89c5bc72e21a86458ee159fb995e86b034b2c82dd41dc84bd23612223b
---

# 深入理解组件

组件是 OAM 应用的重要组成部分，代表一个可运行的单元以及描述，可以是应用服务自身，例如无状态的 Web 服务；也可以是应用所依赖的服务，例如 Redis。组件中封装了应用所需的计算、网络等基本能力，并且是一个个可被复用的模块。通过组件，应用开发者可快速创建应用，并投入应用开发。

## 内置组件总览

<table border={1} cellSpacing={0}>
<thead>
  <tr>
    <th>内置组件名称</th>
    <th>适用场景</th>
  </tr>
</thead>
<tbody>
  <tr>
    <td><b>网络服务</b></td>
    <td>适用于定义可持久运行、可扩展的无状态容器化服务的场景。</td>
  </tr>
  <tr>
    <td><b>有状态服务</b></td>
    <td>适用于需要定义可持久运行、做保存数据的有状态容器化服务的场景。</td>
  </tr>
</tbody>
</table>

### 网络服务

该组件涵盖了 Kubernetes 的 Deployment 和 Service，用于定义可持久运行、可扩展的无状态容器化服务，并支持内外部网络访问。例如：您可使用此组件创建一个 Nginx 服务。

### 有状态服务

该组件包括了 Kubernetes 的 StatefulSet 和 Service，用于定义可持久运行、需做保存数据的有状态容器化服务。例如：您可使用此组件创建一个数据库。

### 参数说明 \{#para}

<table border={1} cellSpacing={0}>
<thead>
  <tr>
    <th>参数</th>
    <th>分类</th>
    <th>说明</th>
  </tr>
</thead>
<tbody>
  <tr>
    <td rowSpan={2}><b>方式</b></td>
    <td>选择</td>
    <td>选择镜像仓库中的镜像，您需要具有项目的管理权限，且平台管理员已在 <b>工具链管理</b> > <b>工具链集成</b> 中集成，并分配给您的项目使用。</td>
  </tr>
  <tr>
    <td>输入</td>
    <td>输入的格式为 <b>仓库地址:镜像版本</b>，例如: index.docker.io/library/ubuntu:latest，需确保输入的镜像支持当前集群的硬件架构，若没有输入镜像版本，则版本默认为 latest。</td>
  </tr>
  <tr>
    <td rowSpan={3}><b>网络访问</b></td>
    <td>协议</td>
    <td rowSpan={2}>具体说明请参考 <a href="/application/building_application/functions/operation_after_creating/network_communications/service/create_service">创建内部路由</a> 中的 <b>端口</b> 参数说明。</td>
  </tr>
  <tr>
    <td>容器端口</td>
  </tr>
  <tr>
    <td>类型</td>
    <td>是否对外暴露端口，可选项包括 <b>仅支持内部访问</b> 及 <b>支持内部访问及外部访问</b>。</td>
  </tr>
  <tr>
    <td><b>环境变量</b></td>
    <td>-</td>
    <td>具体说明请参考 <a href="/application/building_application/functions/application_workloads/deployment_create">创建部署参数说明</a> 中的 <b>环境变量</b> 参数说明。</td>
  </tr>
  <tr>
    <td rowSpan={3}><b>存储卷</b></td>
    <td>类型</td>
    <td><ul><li>存储：创建并选择存储。</li><li>配置：创建并选择配置字典或保密字典。</li><li>空目录：如果设置了容器的存储卷类型为空目录，Pod 被分配到主机上时，会创建空目录，如果容器运行在主机上，空目录会一直存在，如果容器被从主机上删除，空目录会被删除，数据丢失。空目录便于运行在同一个 Pod 下的容器共享文件，也支持单个容器短暂性的在磁盘中写入数据。</li></ul></td>
  </tr>
  <tr>
    <td>相关配置</td>
    <td><ul><li>创建新存储：选择存储类并配置容量。需提前联系平台管理员创建存储类，并分配给所在项目使用。</li><li>创建新配置：具体说明请参考 <a href="/application/building_application/functions/preparation_before_creating/add_configmap">创建配置字典</a> 及 <a href="/application/building_application/functions/preparation_before_creating/add_secret">创建保密字典</a>。</li></ul></td>
  </tr>
  <tr>
    <td>挂载路径</td>
    <td>存储卷挂载的路径。</td>
  </tr>
  <tr>
    <td><b>启动参数</b></td>
    <td>-</td>
    <td>用于在启动组件之外额外执行某些 shell 命令，且需遵循标准的 shell 命令书写格式，例如：<code>/bin/sh -c "while true ; do echo Welcome! ; sleep 10 ; done"</code>，且启动命令中还可引用环境变量，假设已设环境变量 MESSAGE:Welcome! ，则上述示例可改为：<code>/bin/sh -c "while true ; do echo $(MESSAGE); sleep 10 ; done"</code>。</td>
  </tr>
  <tr>
    <td><b>资源限额</b></td>
    <td>-</td>
    <td>具体说明请参考 <a href="/application/building_application/functions/application_workloads/deployment_create">资源请求与资源限制值</a> 。</td>
  </tr>
</tbody>
</table>
