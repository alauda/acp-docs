---
weight: 10
---

# 介绍

**ACP Virtualization With Kubevirt** 与 **Kube-OVN** 进行了深度集成，同时扩展了对虚拟机传统网络需求的支持，并对特定场景进行了优化。

## 优势

- **IPv6 支持**

  完整的 IPv6 支持。

- **固定 IP 支持**

  匹配传统虚拟机使用方式，保证虚拟机重启后 IP 不变。

- **多网络模式支持**

  支持容器网络、SR-IOV 等不同的网络模式，可匹配用户不同的使用场景。
