---
weight: 10
i18n:
  title:
    en: Release Notes
    zh: 更新说明
title: 更新说明
sourceSHA: 1a844f412a06026168bdc88aa54ec2bb0187e04d4ae1744759cfac9930614a2f
---

# 更新说明

## 小版本：4.0.0

### 功能与增强

#### 日志查询逻辑优化

日志查询页面进行了优化，以解决用户在使用日志查询功能时遇到的体验和性能问题：

- 原有的单选框已被替换为高级搜索组件。现在，您可以像使用 GIT 搜索一样使用日志搜索。
- 日志内容的独立查询条件。
- 时间查询条件的位置已被调整。现在，您在调整时间范围时不会重置日志过滤条件。
- 优化了日志查询 API，以提高整体查询性能。

#### ElasticSearch 升级至 8.17

我们将 ElasticSearch 的版本升级至 8.17，以跟进社区的功能和增强。

#### ALB 身份验证

ALB 现在支持多种身份验证机制，允许用户在 Ingress 层面处理身份验证，而无需在每个后端应用中实现。

#### ALB 支持 ingress-nginx 注释

本次发布在 ALB 中增加了对常用 ingress-nginx 注释的支持，包括 keepalive 设置、超时配置和 HTTP 重定向，增强了与社区 ingress-nginx 的兼容性。

#### Kubevirt 实时迁移优化

在实时迁移过程中，网络中断时间已减少到 0.5 秒以下，现有的 TCP 连接不会断开。这项优化显著提高了生产环境中虚拟机迁移的稳定性和可靠性。

#### LDAP/OIDC 集成优化

LDAP/OIDC 集成表单字段已被调整，主要包括删除不必要/重复字段和优化字段描述。现在，LDAP/OIDC 集成支持通过 YAML 进行配置，允许在 YAML 文件中映射用户属性。

### 源代码到镜像 (Source to Image, S2I) 支持

- 添加了 **ACP Builds** 运算符，用于自动从源码构建镜像。
- 支持 Java/Go/Node.js/Python 语言栈。
- 简化了通过源代码仓库的应用部署。

### 本地 Registry 解决方案

- **ACP Registry** 提供轻量级 Docker Registry，具有企业级功能。
- 提供开箱即用的镜像管理能力。
- 简化了应用交付。

### GitOps 模块重构

- 将 **ACP GitOps** 解耦为独立的集群插件架构。
- 已将 Argo CD 升级至 v2.14.x 版本。
- 增强了基于 GitOps 的应用生命周期管理。

### 命名空间级监控

- 引入命名空间级动态监控仪表板。
- 提供应用/工作负载/Pods 指标可视化。

### Crossplane 集成

- 发布 **Alauda Build of Crossplane** 发行版。
- 通过 XRD 组合实现以应用为中心的配置。

#### 虚拟化更新

- 升级至 KubeVirt 1.4，增强虚拟化能力。
- 优化镜像处理，快速虚拟机配置。
- 优化虚拟机实时迁移，现在可以直接从 UI 发起，并可见迁移状态。
- 改进网络绑定，支持双栈 (IPv4/IPv6)。
- 增加了 vTPM 支持，以增强虚拟机安全性。

#### Ceph 存储更新

- Metro-DR 与伸缩集群实现跨可用区的实时数据同步。
- Regional-DR 与基于池的镜像增强数据保护。

#### TopoLVM 更新

- 添加对多路径设备部署的支持，提高灵活性和稳定性。

### 修复的问题

可以通过文档框架提供的 release-notes-for-bugs 组件，自动从 JIRA 问题生成 bug 修复列表。

有关使用说明，请参见《自动生成发布说明指南》。

### 已知问题

{/* release-notes-for-bugs?template=aiops_unfixed */}
