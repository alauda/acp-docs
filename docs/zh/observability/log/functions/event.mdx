---
weight: 30
---

# 事件

平台对接了 Kubernetes 事件，记录了 Kubernetes 资源的重要状态变更及各种运行状态变化的事件，并且提供了存储、查询、可视化能力。当集群、节点、Pod 等资源出现异常情况时，可通过事件分析具体原因。

并可基于事件原因为计算组件 [创建告警策略](../../monitor/functions/manage_alert.mdx)，当关键事件发生次数达到告警阈值时，能够自动触发告警并通知相关人员及时处理，以降低平台业务运行风险。


## 操作步骤

1. 在左侧导航栏中，单击 **运维中心** > **事件**。 

    **提示**：通过顶部导航栏的集群切换下拉选择框切换待查看事件的集群。


## 事件概览
    
在事件页面默认展示最近 30 分钟内（可选择或自定义时间范围）的重要事件总览以及资源事件记录。

    
* **重要事件总览**：卡片上展示重要事件的事件原因，以及查询时间范围内发生该类事件的资源个数。
    - **注意**：当同一资源多次发生该类事件时，资源个数不会累加。
    - 例如：节点重启事件的资源个数为 20，表示在查询时间范围内，有 20 个资源发生了该类事件，同一个资源可能发生了多次。
    
    
* **资源事件记录**：重要事件总览区域下方，展示查询时间范围内，符合查询条件的所有事件记录。可通过单击重要事件卡片过滤相应类型的事件，或展开并输入查询条件进行查询。查询条件说明如下：

    * **资源类型**：发生事件的 Kubernetes 资源的类型，例如：Pod。
     
    * **命名空间**：发生事件的 Kubernetes 资源所在的命名空间。
    
    * **事件原因**：事件发生的原因（reason）。

    * **事件级别**：事件的重要程度，如 Warning。
    
    * **资源名称**：发生事件的 Kubernetes 资源的名称。支持选择或输入多个名称。

:::tip
    
- 单击事件记录资源名称右侧的查看图标可在弹出的 **事件详情** 对话框中，查看事件的详细信息。
- 事件原因左侧的图标颜色表示事件等级。绿色图标表示该事件的等级为 `Normal`，可忽略该事件；橘色图标表示该事件的等级为 `Warning`，存在资源异常的情况，需要关注该事件以免引发故障。
    
:::

