---
weight: 40
---

# 用户管理

为确保 Redis 环境安全，请务必设置密码，并妥善保管您的密码。若发现有泄漏风险，请及时更新密码。对于不用业务的使用，可创建不同的用户实现用户权限分离，提高安全性。

**注意**：
1. 对于 Redis 5.0，更新密码会导致实例重启，业务会出现闪断现象。建议您避开业务敏感时期进行操作。
2. 从 Redis 6.0 开始，Redis 支持 ACL 权限控制。


## 用户管理

1. 在左侧导航栏中，单击 **Redis**。

2. 单击 ***命名空间的名称***。

3. 单击 ***实例的名称***。

4. 可在实例的 **用户管理** 页签中管理用户信息及权限。

## 更新密码

### 操作步骤

1. 选择对应用户信息，点击扩展按钮。

2. 选择 **重置密码** ，输入密码，二次输入以确认密码。

3. 等待更新完成即可。

## 更新权限

Redis 内置了如下权限集：

| 权限集       | ACL 规则                            | 描述                                                        |
|:-------------|:------------------------------------|:------------------------------------------------------------|
| NotDangerous | +@all -@dangerous ~*                | 允许对所有 KEY 使用除 dangerous 类之外的所有命令。          |
| ReadWrite    | -@all +@write +@read -@dangerous ~* | 允许对所有键使用除 dangerous 类别之外的所有读取和写入命令。 |
| ReadOnly     | -@all +@read -keys ~*               | 允许对所有 KEY 使用 read 类中的所有命令。                   |
| Administator | +@all -acl ~*                       | 允许对所有 KEY 使用所有命令。                               |

此外，还支持自定义规则。对于自定义规则，您可以参考 Redis 官方文档 [ACL 规则](https://redis.io/topics/acl)。

> 需要注意的是，不管是内置 ACL 规则，还是自定义规则，都强制回收了执行 ACL 命令的权限。需要变更用户，只能在这里进行操作。


### 操作步骤

1. 选择对应用户信息，点击扩展按钮。

2. 选择 **更新权限** 点击。

3. 选择权限集或者点击自定义输入自定义规则。

4. 点击 **更新**，等待更新完成。

## 创建用户

### 操作步骤

1. 点击 **创建用户**。

2. 输入用户名。

3. 输入密码，和重复输入确认密码。

4. 选择权限集或点击自定义输入自定义规则。

5. 点击 **确认**，创建用户。

6. 选择用户，点击用户名可以查看用户状态。

实例状态变为 **运行中** 后，即可使用新密码进行连接调试。


* [访问实例（哨兵模式）]()

* [访问实例（集群模式）]()

